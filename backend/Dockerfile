FROM python:3.9-slim     # устанавливает базовый образ. В данном случае это облегченная (slim) версия Python 3.9 на базе Linux.
WORKDIR /app             # создает рабочую директорию /app внутри контейнера и переходит в неё (аналог команды cd). Все последующие команды будут выполняться в этой папке.
COPY app.py .            # копирует файл app.py с  компьютера в текущую рабочую директорию контейнера 
CMD ["python", "app.py"] # указывает команду, которая запустится автоматически при старте контейнера. В данном случае она запускает скрипт через интерпретатор Python.