# Effective Mobile
### Тестовое задание на позицию DevOps
### Задание: необходимо развернуть простое веб-приложение, доступное пользователю через nginx, запущенные в Docker-контейнерах.
1. Приложение (backend)
Используйте любой вариант:
Вариант A (рекомендуется):
простой HTTP-сервер на Python (http.server) сервер должен отвечать на / текстом:
"Hello from Effective Mobile!"
Вариант B:
любой минимальный HTTP-сервер (Node.js / другое)
Требования: приложение слушает порт 8080
приложение не публикует порт наружу (доступ только из docker-сети)
2. Nginx (reverse proxy)
Используйте официальный образ nginx, nginx должен:
    1. принимать HTTP-запросы на порт 80
    2. проксировать запросы на backend-сервис
    3. использовать отдельный конфигурационный файл
Конфигурация nginx должна:
    1. содержать upstream или прямой proxy_pass
    2. корректно проксировать путь /
3. Docker
Backend
    1. отдельный Dockerfile
    2. приложение запускается при старте контейнера
Nginx
    1. можно использовать стандартный образ
    2. конфигурация подключается через volume или кастомный Dockerfile
4. Docker Compose
Создайте docker-compose.yml, который
    1. поднимает два сервиса:
         backend
         nginx
    2.настраивает сетевое взаимодействие между ними
    3.пробрасывает только порт 80 nginx на хост
    4.задаёт понятные имена контейнерам
5. Проверка работоспособности
     После запуска должно быть возможно выполнить:
     curl http://localhost
    Ожидаемый ответ:
    "Hello from Effective Mobile!"
6. Git
Публичный Git-репозиторий
Понятная структура проекта
├── backend/
│ ├── Dockerfile
│ └── app.py
├── nginx/
│ └── nginx.conf
├── docker-compose.yml
└── README.md    
7. README.md
   Опишите:
   как запустить проект
   как проверить результат
   кратко — как работает схема (nginx → backend)
Ограничения
Использовать только Docker и Docker Compose
Kubernetes использовать нельзя
Не использовать готовые reverse-proxy решения кроме nginx

### Решение: 
# Запуск проекта
![1.Запуск проекта](https://github.com/netgitups/Effective_Mobile_DevOps/blob/main/img/1.png)
# Как проверить результат
![2.Как проверить результат](https://github.com/netgitups/Effective_Mobile_DevOps/blob/main/img/2.png)
# Кратко — как работает схема (nginx → backend)
Пользователь отправляет запрос при помощи curl на http://localhost (порт 80). 
Nginx (контейнер nginx_proxy) принимает запрос, так как его порт 80 проброшен на хост-машину.
Nginx анализирует конфигурацию и перенаправляет запрос (proxy_pass) по внутреннему имени сервиса backend_app на порт 8080 внутри Docker-сети.
Backend (Python-сервер) обрабатывает запрос и возвращает текст.
Nginx передает этот ответ обратно пользователю. Прямого доступа к бэкенду с хоста нет, так как порт 8080 не проброшен.
